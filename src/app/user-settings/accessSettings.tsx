"use client";

import React from "react";
import { Box, Tabs, Tab, Typography } from "@mui/material";

const AccessSettings: React.FC = () => {
  return <div>{/* Add your Password Settings UI here */}</div>;
};

export default AccessSettings;
